# 子系統① 專案概念 + 專案記憶 實現計劃

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推薦）逐任務實現。步驟使用複選框（`- [ ]`）。

**目標：** 建立 `projects` 實體（含記憶帳本）+ session 可歸屬專案 + 專案記憶自動注入該專案的新 session + 精簡 UI（指派下拉 + 記憶面板）。

**架構：** 後端新增 projects 表/store/route；`sessions` 加 `project_id`；`agentManager.start()` 新增 `projectId` 參數，於建立新 session 時注入專案記憶區塊並把 project_id 持久化（複用既有 upsertSession）。前端標頭加專案下拉 + 記憶面板。跨專案共享沿用既有工作區筆記，不另建。

**技術棧：** Node 24 + `node:sqlite`、Express、vitest（server `npm test = tsc --noEmit && vitest`）、React + Vite。

---

## 檔案結構
- 修改 `server/src/dbSchema.ts`：projects 表 + sessions.project_id（BASE_SCHEMA + migration）。
- 建立 `server/src/store/projects.ts`：projects CRUD + 記憶帳本（滾動截斷）。
- 修改 `server/src/store/types.ts`：`SessionRecord` 加 `projectId?`。
- 修改 `server/src/store/sessions.ts`：rowToSession/upsertSession 帶 project_id + `setSessionProject`。
- 修改 `server/src/store.ts`：re-export projects store（沿用既有 barrel 模式）。
- 修改 `server/src/agentManager.ts`：`start()` 加 `projectId` 參數→注入專案記憶 + 持久化。
- 建立 `server/src/routes/projects.ts`：/api/projects CRUD；修改 `server/src/index.ts` 掛載；修改 `server/src/routes/sessions.ts` 的 PATCH /sessions/:id 接受 projectId + POST 建立 session 接受 projectId。
- 測試：`server/src/store/projects.test.ts`、`server/src/app.test.ts`（端點）。
- 前端：`client/src/lib/api.ts`、新 `client/src/hooks/useProjects.ts`、`client/src/components/ChatWindow.tsx`（標頭下拉）、新 `client/src/components/ProjectMemoryModal.tsx`。

---

### 任務 1：DB schema — projects 表 + sessions.project_id

**文件：** 修改 `server/src/dbSchema.ts`

- [ ] **步驟 1：BASE_SCHEMA 加 projects 表**（放在 `sessions` 表定義之後）
```sql
CREATE TABLE IF NOT EXISTS projects (
  id TEXT PRIMARY KEY,
  workspace_id TEXT NOT NULL,
  name TEXT NOT NULL,
  memory TEXT NOT NULL DEFAULT '',
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_projects_ws ON projects(workspace_id, updated_at DESC);
```
並在 BASE_SCHEMA 的 `sessions` 表內 `tags ...` 之後加一欄 `project_id TEXT,`（在 `created_at` 之前）。

- [ ] **步驟 2：applyMigrations 加 idempotent 區塊**（在現有 autonomy_runs/agent_memory 區塊之後）
```ts
  if (!tableExists(db, "projects")) {
    db.exec(`CREATE TABLE projects (
      id TEXT PRIMARY KEY, workspace_id TEXT NOT NULL, name TEXT NOT NULL,
      memory TEXT NOT NULL DEFAULT '', created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL
    );`);
    db.exec("CREATE INDEX IF NOT EXISTS idx_projects_ws ON projects(workspace_id, updated_at DESC)");
  }
  if (tableExists(db, "sessions") && !hasColumn(db, "sessions", "project_id")) {
    db.exec("ALTER TABLE sessions ADD COLUMN project_id TEXT");
  }
```

- [ ] **步驟 3：型別檢查** `cd server && npx tsc --noEmit` → 零錯。
- [ ] **步驟 4：Commit** `git commit -m "feat(projects): add projects table + sessions.project_id"`（結尾加 Co-Authored-By 行）。

---

### 任務 2：store/projects.ts（CRUD + 記憶帳本）

**文件：** 建立 `server/src/store/projects.ts`；測試 `server/src/store/projects.test.ts`

- [ ] **步驟 1：先寫測試**
```ts
import { describe, it, expect } from "vitest";
import { createProject, listProjects, getProject, renameProject, deleteProject, getProjectMemory, setProjectMemory } from "./projects.js";
import { upsertSession, getSession } from "./sessions.js";
import { setSessionProject } from "./sessions.js";

describe("projects store", () => {
  it("create / list / get / rename", () => {
    const p = createProject({ workspaceId: "w1", name: "AudioScape" });
    expect(p.id).toBeTruthy();
    expect(getProject(p.id)!.name).toBe("AudioScape");
    expect(listProjects("w1").some((x) => x.id === p.id)).toBe(true);
    renameProject(p.id, "AudioScape v2");
    expect(getProject(p.id)!.name).toBe("AudioScape v2");
  });
  it("memory set/get + 滾動截斷(8KB)", () => {
    const p = createProject({ workspaceId: "w1", name: "M" });
    expect(getProjectMemory(p.id)).toBe("");
    setProjectMemory(p.id, "決策一");
    expect(getProjectMemory(p.id)).toBe("決策一");
    const big = "x".repeat(9000);
    setProjectMemory(p.id, big);
    expect(getProjectMemory(p.id)!.length).toBeLessThanOrEqual(8192);
  });
  it("刪除專案 → 其下 session 解除綁定(project_id=null)，session 不刪", () => {
    const p = createProject({ workspaceId: "w1", name: "D" });
    const sid = `t_${Date.now()}`;
    upsertSession({ id: sid, workspaceId: "w1", agentId: "x", title: "t", provider: "claude", createdAt: Date.now(), updatedAt: Date.now(), messages: [] });
    setSessionProject(sid, p.id);
    expect(getSession(sid)!.projectId).toBe(p.id);
    deleteProject(p.id);
    expect(getProject(p.id)).toBeUndefined();
    expect(getSession(sid)).toBeTruthy();
    expect(getSession(sid)!.projectId).toBeUndefined();
  });
});
```

- [ ] **步驟 2：跑測試確認失敗** `cd server && npx vitest run src/store/projects.test.ts` → FAIL（模組不存在）。

- [ ] **步驟 3：實現 `server/src/store/projects.ts`**
```ts
import { db } from "../db.js";

export interface Project {
  id: string; workspaceId: string; name: string; memory: string;
  createdAt: number; updatedAt: number;
}

const MEMORY_MAX = 8192;

function rowToProject(r: any): Project {
  return { id: r.id, workspaceId: r.workspace_id, name: r.name, memory: r.memory ?? "", createdAt: r.created_at, updatedAt: r.updated_at };
}

export function createProject(input: { workspaceId: string; name: string }): Project {
  const id = `proj_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 8)}`;
  const now = Date.now();
  db.prepare("INSERT INTO projects (id, workspace_id, name, memory, created_at, updated_at) VALUES (?, ?, ?, '', ?, ?)")
    .run(id, input.workspaceId, input.name, now, now);
  return getProject(id)!;
}
export function getProject(id: string): Project | undefined {
  const r = db.prepare("SELECT * FROM projects WHERE id = ?").get(id) as any;
  return r ? rowToProject(r) : undefined;
}
export function listProjects(workspaceId: string): Project[] {
  return (db.prepare("SELECT * FROM projects WHERE workspace_id = ? ORDER BY updated_at DESC").all(workspaceId) as any[]).map(rowToProject);
}
export function renameProject(id: string, name: string): void {
  db.prepare("UPDATE projects SET name = ?, updated_at = ? WHERE id = ?").run(name, Date.now(), id);
}
export function getProjectMemory(id: string): string | undefined {
  const r = db.prepare("SELECT memory FROM projects WHERE id = ?").get(id) as any;
  return r ? (r.memory ?? "") : undefined;
}
export function setProjectMemory(id: string, content: string): void {
  // 滾動截斷：超過上限保留最新（尾端）。仿 store/workspaces.ts:appendWorkspaceMemory。
  const trimmed = content.length > MEMORY_MAX ? content.slice(content.length - MEMORY_MAX) : content;
  db.prepare("UPDATE projects SET memory = ?, updated_at = ? WHERE id = ?").run(trimmed, Date.now(), id);
}
export function deleteProject(id: string): void {
  db.prepare("UPDATE sessions SET project_id = NULL WHERE project_id = ?").run(id);
  db.prepare("DELETE FROM projects WHERE id = ?").run(id);
}
```

- [ ] **步驟 4：跑測試** `cd server && npx vitest run src/store/projects.test.ts` → PASS（依賴任務 3 的 setSessionProject；若尚未實作則先做任務 3 再回來，或本步驟與任務 3 合併驗證）。
- [ ] **步驟 5：Commit** `git commit -m "feat(projects): projects store with rolling memory ledger"`。

---

### 任務 3：store/sessions.ts + types.ts — project_id 串接

**文件：** 修改 `server/src/store/types.ts`、`server/src/store/sessions.ts`

- [ ] **步驟 1：types.ts 的 SessionRecord 加欄**（第 11-23 行 interface 內，`tags?` 之後）
```ts
  projectId?: string;
```

- [ ] **步驟 2：sessions.ts `rowToSession` 加映射**（在 `tags: parseTags(r.tags),` 之後）
```ts
    projectId: r.project_id || undefined,
```

- [ ] **步驟 3：upsertSession 帶 project_id**
  - UPDATE 語句加 `project_id = ?`（與其他欄並列），對應參數加 `s.projectId ?? null`。
  - INSERT 語句欄位加 `project_id`、VALUES 加一個 `?`，參數加 `s.projectId ?? null`。
  （注意欄位數與 `?` 數一致。）

- [ ] **步驟 4：新增 setSessionProject（檔尾）**
```ts
export function setSessionProject(sessionId: string, projectId: string | null): void {
  db.prepare("UPDATE sessions SET project_id = ? WHERE id = ?").run(projectId, sessionId);
}
```

- [ ] **步驟 5：型別檢查 + projects 測試一起綠** `cd server && npx tsc --noEmit && npx vitest run src/store/projects.test.ts` → PASS。
- [ ] **步驟 6：Commit** `git commit -m "feat(projects): persist project_id on sessions + setSessionProject"`。

---

### 任務 4：agentManager.start() 注入專案記憶 + 持久化 project_id

**文件：** 修改 `server/src/agentManager.ts`

- [ ] **步驟 1：import getProjectMemory**（檔頭既有 store import 區塊加）
```ts
import { getProjectMemory } from "./store/projects.js";
```

- [ ] **步驟 2：start() 簽名加 projectId 參數**（第 173-180 行）
在 `provider: Provider = "claude",` 之後加 `projectId?: string,`。

- [ ] **步驟 3：注入專案記憶區塊**（在 agentMemoryBlock 之後、約第 197 行後加）
```ts
    // 專案記憶：若此 session 歸屬某專案，注入該專案的持久記憶/狀態。
    let projectMemoryBlock = "";
    if (projectId) {
      const pm = getProjectMemory(projectId);
      if (pm && pm.trim()) {
        projectMemoryBlock = `\n\n# 本專案的記憶 / 狀態\n以下是這個專案累積的決策、狀態與脈絡，請以此為前提繼續：\n\n${pm.trim()}\n`;
      }
    }
```
並把 `projectMemoryBlock` 串接進最終 system prompt 組裝處（與 memoryBlock / agentMemoryBlock / craftBlock 並列加入）。

- [ ] **步驟 4：持久化 project_id**（start() 內第 286 行的 `upsertSession({...})` 物件加上）
```ts
      projectId,
```

- [ ] **步驟 5：型別檢查 + 全套** `cd server && npm test` → 全綠（既有 start() 呼叫端不傳 projectId → undefined，行為不變，向後相容）。
- [ ] **步驟 6：Commit** `git commit -m "feat(projects): inject project memory + persist project_id in agentManager.start"`。

---

### 任務 5：API — projects route + 掛載 + session 指派

**文件：** 建立 `server/src/routes/projects.ts`；修改 `server/src/index.ts`、`server/src/routes/sessions.ts`；測試 `server/src/app.test.ts`

- [ ] **步驟 1：先寫端點測試**（app.test.ts，沿用既有 base/fetch 模式）
```ts
it("projects CRUD + 指派 session", async () => {
  const c = await fetch(`${base}/api/projects?workspace=${DEFAULT_WORKSPACE_ID}`, {
    method: "POST", headers: { "content-type": "application/json" },
    body: JSON.stringify({ name: "E2E 專案" }),
  });
  expect(c.status).toBe(200);
  const { project } = await c.json() as { project: { id: string } };
  expect(project.id).toBeTruthy();
  // 更新記憶
  const u = await fetch(`${base}/api/projects/${project.id}`, {
    method: "PATCH", headers: { "content-type": "application/json" },
    body: JSON.stringify({ memory: "本專案重點" }),
  });
  expect(u.status).toBe(200);
  // 指派 session
  const sid = `t_proj_${Date.now()}`;
  upsertSession({ id: sid, workspaceId: DEFAULT_WORKSPACE_ID, agentId: "x", title: "t", provider: "claude", createdAt: Date.now(), updatedAt: Date.now(), messages: [] });
  createdSessionIds.push(sid);
  const a = await fetch(`${base}/api/sessions/${sid}`, {
    method: "PATCH", headers: { "content-type": "application/json" },
    body: JSON.stringify({ projectId: project.id }),
  });
  expect(a.status).toBe(200);
  expect(getSession(sid)!.projectId).toBe(project.id);
});
```
（import 既有 `getSession` from "./store.js" 若尚未 import 則加。）

- [ ] **步驟 2：跑測試確認失敗** `cd server && npx vitest run src/app.test.ts -t "projects CRUD"` → FAIL（路由不存在）。

- [ ] **步驟 3：建立 `server/src/routes/projects.ts`**
```ts
import { Router } from "express";
import { createProject, listProjects, getProject, renameProject, deleteProject, setProjectMemory } from "../store/projects.js";
import { DEFAULT_WORKSPACE_ID } from "../db.js";

export const projectsRouter = Router();

function ws(req: any): string {
  return String(req.query.workspace || req.get("x-workspace") || DEFAULT_WORKSPACE_ID);
}

projectsRouter.get("/", (req, res) => res.json({ projects: listProjects(ws(req)) }));
projectsRouter.post("/", (req, res) => {
  const name = String(req.body?.name || "").trim();
  if (!name) return res.status(400).json({ error: "name 不可空" });
  res.json({ project: createProject({ workspaceId: ws(req), name }) });
});
projectsRouter.patch("/:id", (req, res) => {
  const p = getProject(req.params.id);
  if (!p) return res.status(404).json({ error: "專案不存在" });
  if (typeof req.body?.name === "string" && req.body.name.trim()) renameProject(p.id, req.body.name.trim());
  if (typeof req.body?.memory === "string") setProjectMemory(p.id, req.body.memory);
  res.json({ project: getProject(p.id) });
});
projectsRouter.delete("/:id", (req, res) => {
  if (!getProject(req.params.id)) return res.status(404).json({ error: "專案不存在" });
  deleteProject(req.params.id);
  res.json({ ok: true });
});
```

- [ ] **步驟 4：index.ts 掛載**（找既有 `app.use("/api/...", ...Router)` 區塊，依樣加）
```ts
import { projectsRouter } from "./routes/projects.js";
// ...與其他 router 並列
app.use("/api/projects", projectsRouter);
```

- [ ] **步驟 5：sessions.ts 的 PATCH /sessions/:id 接受 projectId**
讀既有 `sessionsRouter.patch("/sessions/:id", ...)`（約第 88-100 行），在其更新邏輯加：若 `req.body.projectId !== undefined` 則 `setSessionProject(req.params.id, req.body.projectId || null)`（import setSessionProject from "../store.js" 或 "../store/sessions.js"）。保留既有 title/tags 更新行為。

- [ ] **步驟 6：跑測試** `cd server && npm test` → 全綠（含新端點測試）。
- [ ] **步驟 7：Commit** `git commit -m "feat(projects): projects API + session assignment route"`。

---

### 任務 6：前端 — 專案下拉 + 記憶面板

**文件：** 修改 `client/src/lib/api.ts`、`client/src/components/ChatWindow.tsx`；建立 `client/src/hooks/useProjects.ts`、`client/src/components/ProjectMemoryModal.tsx`

> 動手前先讀 `client/src/lib/api.ts`（fetch 慣例）、`ChatWindow.tsx` 標頭區（約第 209-233 行那排按鈕）、`AgentMemoryModal.tsx`（modal 樣式範本）。下列為精確契約。

- [ ] **步驟 1：api.ts 加呼叫**
```ts
// GET /api/projects?workspace= ; POST /api/projects {name} ; PATCH /api/projects/:id {name?,memory?} ; DELETE
// PATCH /api/sessions/:id {projectId}
```
依 api.ts 既有風格加 `listProjects(ws)`, `createProject(ws,name)`, `updateProject(id,{name?,memory?})`, `deleteProject(id)`, `setSessionProject(sessionId, projectId|null)`。

- [ ] **步驟 2：useProjects(workspaceId) hook**：load 專案清單 + create/update/delete + refresh，仿既有 `useNotes`/`useAutonomy` 結構。

- [ ] **步驟 3：ChatWindow 標頭加「📁 專案 ▾」下拉**：列出專案 + 「＋ 新專案」+「未分類」；選擇呼叫 `api.setSessionProject(sessionId, projectId|null)` 後刷新；顯示目前 session 所屬專案名。

- [ ] **步驟 4：ProjectMemoryModal**：開啟顯示某專案 memory（textarea 可編輯）+ 儲存呼叫 `api.updateProject(id,{memory})`。標頭下拉旁一個「記憶」入口開啟之。沿用 AgentMemoryModal 樣式。

- [ ] **步驟 5：client 型別檢查** `cd client && npx tsc -b` → 零錯。
- [ ] **步驟 6：Commit** `git commit -m "feat(projects): client project selector + memory panel"`。

---

### 任務 7：真瀏覽器 e2e

- [ ] **步驟 1**：開 PM → 標頭「📁 專案」→「＋ 新專案」建「AudioScape」。
- [ ] **步驟 2**：把目前 session 指派到 AudioScape；開「專案記憶」面板，輸入「主題：空間音訊；上次結論：用 ambisonics」儲存。
- [ ] **步驟 3**：在該專案開一條**新** PM session（建立時帶 projectId）→ 問「我們這個專案上次聊到什麼？」→ PM 回覆能引用「空間音訊 / ambisonics」（驗證專案記憶注入）。
- [ ] **步驟 4**：未分類的舊 session 行為不變（無專案記憶注入）。

---

## 自檢結果

**規格覆蓋度：** §3.1 資料模型→任務1；§3.2 store→任務2+3；§3.3 注入→任務4；§3.4 API→任務5；§3.5 UI→任務6；§5 驗證→各任務測試+任務7。全覆蓋。

**佔位符掃描：** server 核心（schema/store/注入/route）皆含完整碼。任務6 client 依既有元件樣式，給精確 API 契約 + 指定先讀檔（非佔位符，與前述已完成功能同模式）。

**類型一致性：** `Project`、`getProjectMemory`/`setProjectMemory`/`setSessionProject`/`createProject` 等跨任務命名一致；`SessionRecord.projectId`（types→rowToSession→upsertSession→agentManager 持久化→API 回傳）一條線一致；start() 的 projectId 參數為 optional，既有呼叫端不傳→undefined→向後相容。

**已知設計取捨：** 專案記憶只在「新開 session」時注入（沿用既有 workspace/agent 記憶僅 new-session 注入的行為，第 227-229 行已記載）；對既有 session 事後改指派只影響分組與「之後新開的 session」，不重建進行中對話的 prompt。
