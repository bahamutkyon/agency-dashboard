node_modules/
dist/
.DS_Store
*.log
server/data/
*.migrated
.vscode/
.idea/
.claude/

# 環境變數(個人設定:Tailscale IP / token / API key 等不能上 git)
.env
.env.local
.env.*.local

# Playwright MCP 工作目錄(agent 操作產生的暫存,含可能含個人資料的截圖)
.playwright-mcp/
server/.playwright-mcp/
server/*.png
server/*.jpg
server/*.jpeg
server/x*.json
server/*-base64.json
server/fb-*.yml
server/fb-*.json
server/spec*.yml

# Build 產物(每次 build 都會重生,進 repo 只會製造 diff 噪音)
client/tsconfig.tsbuildinfo

# Dev server 日誌
server/dev-output.txt
.dashboard-logs/

# 早期測試殘留：根目錄 data/ 是廢棄路徑（實際 DB 在 server/data/）
/data/

# 一次性 dump/log 含使用者 memory 內容，不進 repo
scripts/legacy-dump.json
scripts/classify-result.json
scripts/execute-log.json

# 升級前自動產生的備份檔
*.pre-awesome.bak

# Awesome skills 整目錄(9.3MB,本機已裝於 ~/.claude/skills/)
skills-awesome-zh/

